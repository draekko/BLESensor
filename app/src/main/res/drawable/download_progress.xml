<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <color android:color="@color/blegadget_grey_light"/>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="6.5dp" />

                <gradient
                    android:angle="90"
                    android:centerColor="@color/blegadget_blue"
                    android:centerY="0.4"
                    android:endColor="@color/blegadget_blue_darkened"
                    android:startColor="@color/blegadget_blue_darkened"/>
            </shape>
        </clip>
    </item>
</layer-list>